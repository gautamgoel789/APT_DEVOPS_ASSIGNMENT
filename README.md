# APT_DEVOPS_ASSIGNMENT

Prerequisites: AWS CLI Installed 
cmd : aws -version 
cmd : aws configure 
Provide: AWS Access Key 
AWS Secret Key 
Region (ap-south-1 recommended)

Terraform Installed cmd : terraform -version

Deployment Steps : Run the deploy script: ./scripts/deploy.sh

Testing steps: Run: ./scripts/test.sh <ALB_DNS>

Run the destroy script: Run : ./scripts/destroy.sh
